#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

    int term = 6;
    int diff = 3;

    for(int i = 0; i < n; i++) {
        printf("%d ", term);
        term += diff;
        diff += 2;
    }

    return 0;
}
